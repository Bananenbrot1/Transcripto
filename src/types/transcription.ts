// Re-export shared types so existing renderer imports continue to work.
export type {
  AudioSource,
  RecordingState,
  TranscriptSegment,
  DiarizationSegment,
  ModelDefinition,
  ModelStatus,
  TranscribeResult,
  DownloadProgress,
} from '../../shared/types';
